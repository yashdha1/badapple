# ASCII Video Player

Frames bloom into light,  
Terminal hums in time,  
Shadows learn to sing.

## Run

```powershell
uv run python main.py
```

## Download a Video

```powershell
uv run python main.py --add "<video-link>"
```

## Controls

- `Space` / `P`: pause or play
- `V`: next ASCII mode
- `T`: next theme
- `F`: fullscreen video-only
- `H`: toggle HUD
- `Q` / `Esc`: quit
